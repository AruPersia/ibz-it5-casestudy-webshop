{% for category in categories %}
    <a href="{{ path('frontendShowCategory', {'categoryPath': category.path}) }}"
       class="list-group-item list-group-item-action">{{ category.name|capitalize }}</a>
{% endfor %}