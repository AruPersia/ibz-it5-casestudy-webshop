<h5>Articles</h5>
<ul>
    {% for category in categories %}
        <li>
            <a href="{{ path('frontendShowCategory', {'categoryPath': category.path}) }}">{{ category.name }}</a>
        </li>
    {% endfor %}
</ul>