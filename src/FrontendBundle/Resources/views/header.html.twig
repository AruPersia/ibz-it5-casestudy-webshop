<nav class="navbar navbar-toggleable-md navbar-light bg-faded">

    <button class="navbar-toggler navbar-toggler-right"
            type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">

        <span class="navbar-toggler-icon"></span>

    </button>

    <a class="navbar-brand" href="{{ path('catalogue') }}">Navbar</a>

    <div class="collapse navbar-collapse">

        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link" href="{{ path('catalogue') }}">Home</a>
            </li>
            <li class="navbar-nav mr-auto">
                <a class="nav-link" href="{{ path('registration') }}">Join now</a>
            </li>
        </ul>

        <form class="form-inline my-2 my-lg-0">
            <input class="form-control mr-sm-2" type="text" placeholder="Search">
            <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
        </form>

        <ul class="navbar-nav">
            <li class="navbar-nav mr-auto">
                {% if app.user %}
                    <ul class="nav navbar-nav pull-right">
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" data-toggle="dropdown"> {{ app.user.email }} </a>
                            <div class="dropdown-menu">
                                <span class="dropdown-item"><b>First name:</b> {{ app.user.firstName }}</span>
                                <span class="dropdown-item"><b>Last name:</b> {{ app.user.lastName }}</span>
                                <span class="dropdown-item"><b>Roles:</b> {{ app.user.roles|join(', ') }}</span>
                                <a class="dropdown-item" href="{{ path('logout') }}"><i
                                            class="fa fa-edit"></i>Logout</a>
                            </div>
                        </li>
                    </ul>
                {% else %}
                    <a class="nav-link" href="{{ path('login') }}">Login</a>
                {% endif %}
            </li>
        </ul>

    </div>

</nav>