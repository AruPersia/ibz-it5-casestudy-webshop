{% if breadcrumbs is defined %}
    <ol class="breadcrumb">
        {% for breadcrumb in breadcrumbs %}
            <li class="breadcrumb-item">{{ breadcrumb }}</li>
        {% endfor %}
    </ol>
{% endif %}