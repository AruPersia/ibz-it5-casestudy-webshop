{% extends '@Frontend/category.html.twig' %}

{% block title %}Join us{% endblock %}

{% block content %}

    <div class="container">
        <div class="row">
            <div class="col-lg-4"></div>
            <div class="col-lg-4 ws-panel">
                {{ form(loginForm, {'title': 'Login', 'action': path('login_verify')}) }}
            </div>
            <div class="col-lg-4"></div>
        </div>
    </div>

{% endblock %}