<nav class="navbar navbar-fixed-top navbar-toggleable-sm navbar-inverse bg-primary mb-2">

    <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse"
            data-target="#collapsingNavbar">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="flex-row d-flex">
        <div class="navbar-brand mb-1" href="#">ADMINISTRATOR</div>
    </div>

    <div class="navbar-collapse collapse" id="collapsingNavbar">

        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link" href="{{ path('backend_dashboard') }}">Dashboard</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#">Category</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('backend_products') }}">Product</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('backend_orders') }}">Orders</a>
            </li>
        </ul>

        <ul class="navbar-nav ml-auto">
            {% if app.user %}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" data-toggle="dropdown"> {{ app.user.email }} </a>
                    <div class="dropdown-menu">
                        <a class="dropdown-item" href="{{ path('backend_logout') }}">
                            <i class="fa fa-pencil"></i> Logout
                        </a>
                        {% for role in app.user.roles %}
                            <div class="dropdown-item disabled">
                                <span class="badge badge-primary">{{ role }}</span>
                            </div>
                        {% endfor %}
                    </div>
                </li>
            {% else %}
                <li>
                    <a class="nav-link" href="{{ path('backend_login') }}">Login</a>
                </li>
            {% endif %}
        </ul>

    </div>
</nav>