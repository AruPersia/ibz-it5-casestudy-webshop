{% extends '@Backend/base.html.twig' %}

{% set title = 'Login as an administrator' %}

{% block body %}

    <div class="container">
        <div class="row">
            <div class="col-lg-4"></div>
            <div class="col-lg-4 ws-panel">
                {{ form(loginForm, {'title': 'Login', 'action': path('backend_submit_login')}) }}
            </div>
            <div class="col-lg-4"></div>
        </div>
    </div>

{% endblock %}